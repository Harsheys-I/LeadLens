{
  "version": "2.7.0",
  "name": "LeadLens",
  "notes": "Estimated run time; upload column-vs-settings check; new Connected Yes/No defaults; first-run API key onboarding + validation; friendlier requirement rule; batch max 20 / parallel max 50"
}
