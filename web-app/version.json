{
  "version": "3.0.2",
  "name": "LeadLens",
  "notes": "Parallel TeleCaller Review: up to 10 review jobs at once (worker pool); remaining queued. Per-job pause/checkpoints unchanged. Removed debug ingest."
}
